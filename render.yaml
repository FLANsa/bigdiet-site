services:
  - type: web
    name: big-diet-system
    env: static
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
